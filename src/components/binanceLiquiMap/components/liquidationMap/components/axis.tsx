const Axis = ({ children }) => {
  // In future, add lines for axis - for example:
  // Highest short
  // Average short
  // Highest long
  // Average long
  return <svg className="h-full w-full bg-amber-200">{children}</svg>;
};

export default Axis;
